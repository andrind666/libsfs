{
     "name": "Ubuntu Dev Container",
     "image": "ubuntu:22.04",  # Gunakan image Ubuntu resmi dari Docker Hub
     "features": {
       "ghcr.io/devcontainers/features/docker-in-docker:2": {},  # Opsional: Untuk Docker-in-Docker
       "ghcr.io/devcontainers/features/git:1": {}  # Opsional: Install Git
     },
     "customizations": {
       "vscode": {
         "extensions": ["ms-vscode.cpptools"]  # Contoh: Tambahkan ekstensi VS Code
       }
     }
   }
